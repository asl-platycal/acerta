# core

Regras centrais e engines (placeholders). **Não depende de React, DOM nem camada de renderização.** Eventos tipados e validadores em `events`; domínio conceitual em `domain`; tipos transversais em `types`; políticas em `rules`.
