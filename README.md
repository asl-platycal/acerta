# ACERTA — acerta-game

Monorepositório da aplicação: app web em `apps/web`; regras e domínio em `core`; integrações em `services`; contratos e utilitários compartilhados em `shared`; Supabase em `backend`; testes em `tests`; referência arquitetural em `docs`.
