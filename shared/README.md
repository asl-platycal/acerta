# shared

Código e dados reutilizáveis entre cliente e servidor. `schemas` centraliza contratos alinhados ao backend; `constants`, `configs` e `utils` evitam duplicação entre `apps/web`, `core` e `services`.
